:: P:person(best_friend => Q:person) | get_along(P,Q).
delay_check(person)?

cleopatra <| person.
:: cleopatra(nose => pretty, occupation => queen).

julias <| person.
:: julias(nose => big, last_name => ceasar).

get_along(cleopatra,julias).
get_along(julias, cleopatrica).

A = person?
A = @(nose => pretty)?
