A=children(int)?

A=children(built_in)?

A=children(real)?

A=children(string)?

A=children("wer")?

A=children(23)?

A=children(23.3)?

A=children(list)?

A=children(bool)?

A=children(true)?

A=children(false)?

A=children(nil)?

A=children(cons)?

A=children(@)?

